.env
init.sh

.env
# Local .terraform directories
**/.terraform/*

# Terraform state files
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Variable files with sensitive data (optional)
# Comment out if you store variables in version control intentionally
*.tfvars
*.tfvars.json

# Override files (used for local config overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# Plan files (binary files generated by 'terraform plan -out')
*.tfplan

# Lock file (optional)
# You might keep this if you want consistent provider versions across machines
# Remove the '#' if you prefer to ignore it
# .terraform.lock.hcl
# Local .terraform directories
**/.terraform/*

# Terraform state files
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Variable files with sensitive data (optional)
# Comment out if you store variables in version control intentionally
*.tfvars
*.tfvars.json

# Override files (used for local config overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# Plan files (binary files generated by 'terraform plan -out')
*.tfplan

# Lock file (optional)
# You might keep this if you want consistent provider versions across machines
# Remove the '#' if you prefer to ignore it
# .terraform.lock.hcl

# my binary file for terraform
executable